class ExampleContextManager:
    """
        We'll fill this out as we follow along with
        the video.
    """
    def __enter__(self):
        pass

    def __exit__(self, exc_type, exc_val, exc_tb):
        return
